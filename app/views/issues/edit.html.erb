<h1>Edit issue</h1>

<%= flash[:error] %>

<%= form_with model: [@team, @issue] do |f| %>
  <%= f.text_field :title %><br>
  <%= f.text_area :body %><br>
  <%= f.collection_check_boxes :tag_ids, @team.tags, :id, :name, include_hidden: false %>
  <%= f.submit %>
<% end %>
