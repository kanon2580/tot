<script>
var ctx = document.getElementById("totalChart");
var totalChart = new Chart(ctx, {
  type: 'radar',
  data: {
    labels: ["返信速度", "所要時間", "いいね", "ベストアンサー", "閲覧数"],
    
    datasets: [{
      label: gon.user_name,
      backgroundColor: 'rgba(255,180,153,0.4)',
      borderColor: 'rgba(255,127,80,1)',
      borderWidth: 1,
      pointRadius: 2,
      data: gon.user_scores
    }]
  }
});
</script>
